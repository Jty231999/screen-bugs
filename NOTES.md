# 开发笔记

面向维护者的记录。这里只写**为什么这么做**、**踩过什么坑**、**实测数据** ——
不重复 README 里已有的介绍。

---

## 目录

- [实测数据](#实测数据)
- [安装后自动启动](#安装后自动启动)
- [界面](#界面)
- [振动反馈](#振动反馈)
- [数量上限与性能](#数量上限与性能)
- [「每个品种最多 5 只」是怎么保证的](#每个品种最多-5-只是怎么保证的)
- [真实感是怎么做的](#真实感是怎么做的)
- [用真实照片数据校正模型](#用真实照片数据校正模型)
- [它是怎么实现的](#它是怎么实现的)
- [开发环境的坑](#开发环境的坑)

---

## 实测数据

在 **vivo / Android 16 (API 36) / 1260×2800 @560dpi** 上实测。

### 帧时间

| 虫子数量 | p50 | p90 | p99 | Janky | Missed Vsync |
|---|---|---|---|---|---|
| 1 只 | 9 ms | 10 ms | 11 ms | 0 | 0 |
| 7 只 | 15 ms | 18 ms | 20 ms | 0 | 0 |
| 14 只 | 22 ms | 25 ms | 28 ms | 0 | 0 |
| 22 只 | 24 ms | 28 ms | 32 ms | 0 | 0 |
| **50 只（满）** | **30 ms** | 34 ms | 38 ms | **0** | **0** |

从 22 只加到 50 只（绘制量增加一倍多），p50 只从 24ms 涨到 30ms，且**全档位 0 jank**。

首尾两次"1 只"测量都是 9ms，说明**没有热降频**，帧时间是随数量单调增长的。

### 模型优化的性能代价

腿从 3 条描边线改成 9 个填充路径、蛾蝶多了一对后翅之后：

| 数量 | 优化前 p50 | 优化后 p50 |
|---|---|---|
| 22~25 只 | 22 ms | 24 ms |
| 50 只 | 28 ms | **30 ms** |

50 只满载只涨 2ms —— 自适应帧率和分级渲染把新增开销吸收掉了。

### 覆盖层窗口数

| 虫子数量 | 绘制层 | 触摸替身 | 合计 |
|---|---|---|---|
| 1 ～ 6 只 | 1 | 每种虫子一个 | 2 ～ 7 |
| 7 只以上 | 1 | **6**（上限） | 7 |

**不随虫子数量增长**，最多只有 6 块「虫子大小」的区域会拦截触摸。

### 触感与唤醒锁

- 自然飞行时的振动频率：**0.9 次/秒**（未做近距离过滤时是 3~4 次/秒）
- 主动戳中：**6 次点击全部触发** onFlee
- 唤醒锁正确获取与释放，收网后 `Wake Locks: size=0`，无残留
- 戳中判定没被模型改动破坏：**10 次点击 9 次命中**

---

## 安装后自动启动

### Android 的限制

**"安装完成后自己弹出来"是做不到的** —— 系统不会给应用自己发任何"安装完成"广播，
第三方也拉不起刚装好的包。重启自动启动虽可用 `RECEIVE_BOOT_COMPLETED`，但很多 ROM 会拦。

所以"安装后自动启动"落在两件能做到的事上：

| 场景 | 行为 |
|---|---|
| 用 `tools\install-apk.ps1` 装 | 装完**立刻 `am start`**，并**先用 adb 授好悬浮窗权限再启动** |
| 手动传 APK 点安装 | 点开图标后**不需要再点任何按钮** |

### 「自动放满」的触发机制

用 **versionCode 变化**判断是不是"新装/刚更新"：

```kotlin
// MainActivity.consumeVersionChange()
val last = p.getLong(KEY_LAST_VERSION, -1L)
if (last == current) return false      // 版本没变 -> 不触发
p.edit().putLong(KEY_LAST_VERSION, current).apply()
return true                            // 版本变了 -> 新装或刚更新
```

触发时把数量写成 `FlySpecies.maxTotal()`（50），并延后 700ms 启动服务
（让界面先画出来，避免黑屏期就放虫）。

比"首次启动标志"准：重新打包安装（版本号不变）不会重复触发。

### 没授权时

悬浮窗权限**只能由用户手动打开**（`-g` 也授不了，Android 的安全设计）。
未授权时 `Toast` 说明"点一下回来就会自动放出"，并**主动把授权页推到用户面前**；
用户打开开关返回时 `onResume` 里的 `maybeAutoReleaseAfterGrant()` 接管。

### 踩到的顺序坑

最初把 `appops set SYSTEM_ALERT_WINDOW allow` 放在**启动 App 之后**：

1. 启动 App，此时还没权限 → 放弃自动放出，**但 versionCode 已被消费**
2. 再 force-stop 重启 → 版本号已相同 → 不再触发
3. 结果：**权限明明授好了，虫子却一只没放**

每一步都"看起来正常"，所以很隐蔽。**修正：授权必须在启动之前**
（`安装 → 授权 → 启动`）。

---

## 界面

主界面分五块：**标题 → 实际效果预览 → 主操作按钮 → 权限提示（按需出现）→ 设置**。

### 界面优化时找出的真实问题

| 问题 | 严重度 | 解法 |
|---|---|---|
| **完全没有深色模式适配** | 高 | 主题用 `Theme.Material3.DayNight` 但只有 `values/colors.xml`，系统切深色时文字被改浅、卡片还是白的 → **看不清**。补 `values-night/` 完整配色 + 独立主题 |
| **主按钮未授权时是个死按钮** | 高 | 原来显示「放虫」点了却只弹 toast 再跳授权页。现在未授权时按钮就是「先授予权限」，点了直接去 |
| 权限卡常驻占地方 | 中 | 已授权时整张卡 `GONE` |
| 不知道实际会放出什么 | 中 | 新增效果预览 |
| 布局硬编码中文 | 低 | 全部收进 `strings.xml` |

### 效果预览

`InsectPreviewView` **复用 `FlyActor` 的绘制代码**，同一套品种参数和绘制逻辑，
所以预览里看到的就是真放出去的样子。

- 动画由 `Choreographer` 驱动，`onVisibilityChanged` 里挂/摘回调，**离开界面完全停掉**
- 预览区最多同时画 10 只：框只有 150dp 高，塞满会糊成一团还白耗电
- 体型读**和正式放出同一个常量**，否则预览的相对大小就是假的

### 边到边绘制

Android 15（API 35）起 targetSdk 35 的应用**默认边到边**，内容会顶到状态栏底下。
实测标题顶边只到 y=98，而状态栏占 0..133 —— 确实被压住。

按版本分别处理：API 35+ 用 `setOnApplyWindowInsetsListener` 拿真实 inset，
更早的版本交给系统自动留白。

> 排查时有个插曲：检测一直报"顶部 y=46 有内容"。拿系统桌面做基线对比才看清 ——
> 那是**状态栏自己的图标**，任何 App 都会有。

### 深色模式验证方法

直接采样单个像素会落在字间空白上，不可靠（第一次就误报了"文字不可读"）。
用**区域内最亮/最暗像素 vs 中位亮度**算对比度才对：

| 元素 | 浅色对比度 | 深色对比度 |
|---|---|---|
| 标题 | 168（深字浅底） | 219（浅字深底） |
| 操作提示 | 150 | 144 |
| 品种说明 | 158 | 134 |

### 图标

自适应图标画了一只俯视的家蝇。**约束**：系统会把前景图放进**中心 72×72 安全区**
再套遮罩，所以图形必须自己居中并尽量铺满 108 视口，否则显示出来又小又偏。

---

## 振动反馈

### 六种振感

| 事件 | 手法 |
|---|---|
| 被戳中 | 最重的一次「啪」，`CLICK + THUD` 组合 |
| 逃窜 | 三下快速短振，像翅膀扑腾 |
| 跳跃 | 干脆的「弹」，`TICK + CLICK` 组合 |
| 落地 | 轻微的「落」感 |
| 起飞 | 比落地略强 |
| 开场 / 收网 | 由远及近一串 / 明确的结束感 |

### 实测振幅（按品种分级）

| 品种 | 权重 | 落地 | 起飞 | 受惊 |
|---|---|---|---|---|
| 果蝇 | 0.35 | 48 | 64 | — |
| 虱子 | 0.35 | 48 | 64 | — |
| 蚂蚁 | 0.40 | 49 | 66 | 96 |
| 跳蚤 | 0.45 | — | — | — |
| 衣鱼 | 0.50 | 52 | 73 | — |
| 臭虫 | 0.55 | — | 73 | — |
| 瓢虫 | 0.60 | 58 | 76 | — |
| 家蝇 | 0.80 | 64 | 83 | — |
| 蟋蟀 | 1.05 | 82 | — | 143 |
| 蟑螂 | 1.35 | 108 | 135 | — |

**果蝇（48）和蟑螂（135）差了近 3 倍。**

### 三条克制原则

1. 只报有意义的**瞬间**（落地/起飞/受惊/跳跃），不是每帧
2. 虫群的自然事件**只在靠近触摸替身时才振** —— 未加这个判断时每秒振 3~4 次，
   加上后降到 **0.9 次/秒**。"戳中"和"跳跃"是用户直接造成的，永远给反馈
3. **90ms 限流兜底**，手指划过多只虫子时不会连成一片

### 尊重系统设置

以下情况**完全不振**：系统关掉「触感反馈」、开了勿扰、设备无振动器。

代码里对每种跳过原因都打日志，调试时能区分"没振是因为限流"还是"系统不让"：

```
D HapticPlayer: skip(oneShot): 限流（距上次 53ms < 90ms）
D HapticPlayer: skip(oneShot): 系统不允许（触感反馈关闭 / 勿扰开启）
```

### 附带的修复：屏幕熄灭会杀掉整个效果

调试时踩到：**把手机放几分钟再回来看，一只虫子都不动了**。
虫子画在悬浮层上，屏幕一熄灭，Choreographer 和主循环都停。

现在服务启动时拿 `SCREEN_DIM_WAKE_LOCK`（允许变暗但仍显示），随服务释放。

> 这个问题一开始被**误判成"振动坏了"** —— 因为零振动只是熄屏的表象。
> 加诊断日志后才发现服务在跑、覆盖层也在（7 个），只是画面全静止了。

---

## 数量上限与性能

上限 = `min(品种数 × 每種上限, FlySpecies.MAX_TOTAL)`。要改的话**三处必须一致**：

- `app/src/main/res/layout/activity_main.xml` — `sliderCount` 的 `android:valueTo`
- `MainActivity.MAX_FLIES`
- `FlyService.MAX_FLIES`

### 为什么品种增加而总数不变

品种从 11 增到 16，但**总数上限仍是 50**（不是 16×5=80）。
因为 50 只已经 30ms/帧，再涨会明显掉帧。保持 50 而增加品种
= **一样是 50 只，品种更丰富，性能不变**：

```
11 种 / 50 只 → 6 种各 5 只 + 5 种各 4 只
16 种 / 50 只 → 4 种各 4 只 + 12 种各 3 只     帧时间 30ms（持平）
```

### 让数量多时不卡的两项优化

1. **分级渲染**（`FlySpecies.detailLevel`）：果蝇、蚂蚁、虱子这类只有几十像素的品种
   完全跳过投影、翅脉和扇翅残影 —— 细节根本看不见，但绘制成本一样
2. **自适应主循环周期**：虫子越多一帧的活越重。与其每 8ms 触发一次、
   每次都在做同一帧做不完的活，不如把周期放长到能出完整帧
   （≤8只 8ms、≤16只 10ms、≤32只 13ms、更多 16ms）

另外 `FlyActor` 里所有依赖尺寸的渐变（Shader）都在 `setSize()` 里一次性建好并缓存。
**如果每帧重建，50 只就是每秒几千次对象分配**，必然引发 GC 抖动掉帧。

---

## 「每个品种最多 5 只」是怎么保证的

分配逻辑在 `FlySpecies.allocate(count)`，用**轮转法**而非"把一个品种填满再换下一个"：

```
16 种、要放 5 只  → 前 5 个品种各 1 只
16 种、要放 16 只 → 16 个品种各 1 只
16 种、要放 50 只 → 前 2 个品种各 4 只、后 14 个各 3 只
```

`FlyLayerView`（真正分配）和 `MainActivity`（界面预览）**共用同一个函数**，
所以预览和实际放出来的一定一致。

### 这里踩过一个坑

最初写成 `while (结果数 < n)` 循环里判断 `round < MAX_PER_SPECIES`。
但 `round` 是**外层轮次**，内层每轮都会跑遍所有品种，
所以 `round` 最多只能到 2，**总数被错误地卡在 品种数 × 2**。

正确做法是先算出**每个品种该放几只** = `ceil(n / 品种数)`，再按这个轮数循环。

---

## 真实感是怎么做的

### 解剖结构

| 结构 | 说明 |
|---|---|
| **头 / 胸 / 腹 三段独立** | 不再是整椭圆。胸部最粗（腿和翅长在这里），腹部向后收窄下垂 |
| **六种头型** | `HeadStyle`：蝇类头部几乎全是复眼；蜻蜓复眼极大呈球形；蚁/蟑螂的头可自由转动（有细颈）；甲虫/蚊小而尖；蛾/蝶/蟋蟀宽圆带绒毛；臭虫/虱/衣鱼扁而宽 |
| **两对翅膀** | 蛾蝶是**四翅**（前翅窄长 + 后翅圆阔），后翅挂点靠后靠外。蜻蜓也是四翅 |
| **股节膨大** | 腿三节粗细不同（股节最粗、胫节中等、跗节最细），用「两端细中间粗」的填充形状画 |
| **两种特殊形态** | `WINGLESS`（臭虫/虱/衣鱼，背面就是分节腹部）和 `FLEA`（侧扁，俯视 3:1 窄长条） |

### 表面质感

| 手法 | 说明 |
|---|---|
| **金属虹彩** | 绿豆蝇/蟑螂/蜻蜓的甲壳有两条不同色温的反光带 |
| **扇翅残影** | 高频扇动的品种叠一层相位偏移的半透明翅膜 —— 真实昆虫扇翅频率远高于屏幕刷新率，肉眼看到的本就是模糊的，所以残影比一片锐利翅膀更像真的 |
| **复眼** | 深色底 + 小眼面横竖纹理 + 高光点 |
| **品种纹理** | 鞘翅中缝、瓢虫 7 斑、蜜蜂黄带、蝶翅眼斑、蛾翅波纹 |
| **触角四型** | 细长 / 羽状带细齿 / 短棒带芒（蝇类）/ 肘状（蚂蚁）/ 蜜蜂的肘状 |

> 触角根位置是**跟着头型算出来的**，不是写死坐标 —— 否则换了头型触角会飘在头外面。

---

## 用真实照片数据校正模型

### 方法

因为**没法"看一眼照片照着画"**（模型无图像输入），所以走了两条可量化的路：

1. **从分类学文献提取解剖测量值** —— 派研究代理搜真实背面照和文献，提取可编码的硬数据
2. **从照片像素程序化提取轮廓** —— `tools/compare-silhouette.ps1`

### 被数据纠正的错误

| 原来的画法 | 真实数据 |
|---|---|
| 蜜蜂：**黄底 + 黑条纹** | **深色腹 + 3 条黄带在腹节前缘**（terga 2–4）— 方向完全反了 |
| 蜜蜂：胸部光头 | 胸部是**金褐色绒毛团**，明显比腹部浅、更蓬松 |
| 瓢虫：圆形 | 宽:长 = **0.77**，是椭圆不是圆 |
| 瓢虫：斑点随意摆 3 个 | 固定 **7 枚**：每鞘翅 3 枚 + 1 枚**盾片斑**跨中缝 |
| 瓢虫：前胸纯黑 | 黑底 + **2 枚白斑**；头还有 2 枚白色额斑 |
| 蟑螂：无条纹 | **前胸两条深色纵纹贯穿全长** |
| 蟑螂：长宽比 ~1.7 | **2.6:1**（13mm × 5mm） |
| 蟑螂：头露在外面 | 真实蟑螂**背面看不到头**，被前胸盖住 |
| 蟋蟀：头比前胸窄 | 头**几乎和前胸等宽甚至略宽**；前胸**宽大于长**（0.75:1） |
| 蟋蟀：光背 | **前胸 5 个深色斑** + 复眼间**深色横带** |
| 蜻蜓：腹部短 | 腹占体长 **72%**；S1 绿、S2–10 蓝 + **粗黑背中线** |

### 关键数据来源

| 昆虫 | 硬数据 | 来源 |
|---|---|---|
| 瓢虫 | 长 5.20–8.60 / 宽 4.00–6.60 mm，宽长比 0.77，7 斑 | [Poorani 2023, Zootaxa 5332:82–87](http://treatment.plazi.org/GgServer/xml/03C7416214094661BDDF3093FD29F992) |
| 瓢虫 | 盾片斑倒心形跨中缝；前胸 2 白斑 | [Vermont Atlas of Life](https://val.vtecostudies.org/projects/lady-beetle-atlas/seven-spotted-lady-beetle/) |
| 蟑螂 | 13 × 5 mm（L:W 2.6:1）；前胸两纵纹；体表油亮 | [UF/IFAS IN128](https://ask.ifas.ufl.edu/publication/in128) |
| 蟋蟀 | 触角长于体长；前胸宽大于长；头≈前胸宽 | [UWyo Gryllus fact sheet](https://www.uwyo.edu/entomology/grasshoppers/colorado/fact-sheets/gryllus.html) |
| 臭虫 | 前胸**宽 = 长的 2.72–2.94 倍**，侧臂前伸到接近复眼 | [Jadidoleslami 2024, JAD 18(1)](https://publish.kne-publishing.com/index.php/JAD/article/view/15669) |
| 虱子 | **腹部边缘暗黑色**（体虱无此深色侧片） | 绿威 虱形态 |
| 跳蚤 | **侧扁**，2–3 × 0.6–1.0 mm → 俯视 **3:1** | [Dimensions.com](https://www.dimensions.com/element/cat-flea-ctenocephalides-felis) |
| 跳蚤 | **颊栉 8–9 枚刺** + **前胸栉**，全部向后指 | [UQ flea key](https://shire.science.uq.edu.au/parasites/insects/fleas/fleas-key.php) |
| 蜻蜓 | 腹占体长 0.72、翼展/体长 1.36；复眼背面相接 | [bumblebee.org](https://www.bumblebee.org/invertebrates/OdonataDragonflies2.htm) |
| 蜜蜂 | 工蜂前翅 7.5–10 mm；黄带在 terga 2–4 前缘 | [Exotic Bee ID](https://www.idtools.org/exotic_bee/index.cfm?packageID=1183&entityID=9011) |

### 轮廓对比工具

`tools/compare-silhouette.ps1`：把任意真实昆虫照片的背面轮廓提取成
「归一化宽度剖面」，再和模型逐点对比。

```powershell
pwsh -File tools\compare-silhouette.ps1 -RefPath <真实照片> -MinePath <App截图>
```

**为什么用"逐行宽度剖面"**：昆虫背面识别度最高的是**轮廓**，
而轮廓可以用"沿体轴各处的宽度"完整描述。这个量对光照、颜色、纹理都不敏感，
只反映形状 —— 比肉眼看"像不像"客观得多。

支持透明 PNG（自动按 alpha 抠图）和纯色背景照片。

### 工具本身的坑

1. **参数名 `-Ref` 和 PowerShell 自动变量 `$Ref` 冲突** → 参数被静默忽略，
   函数根本没被调用。改用 `-RefPath` 才正常。
   **给 ps1 参数命名要避开 PowerShell 自动变量**（`$Ref`/`$Args`/`$Input`/`$Error` 等）
2. 判断数组属性别用 `-not $obj.Profile` —— 数组的真值判断在管道上下文会出意外，
   用 `@($obj.Profile).Count -lt 21` 显式计数

---

## 它是怎么实现的

### 触摸穿透（最关键的设计）

绘制层加 `FLAG_NOT_TOUCHABLE`（否则会吃掉整块屏幕的触摸），
但这样它也收不到触摸事件、无法判断"戳没戳中"。

解法是**两个窗口**：

| 窗口 | 尺寸 | 作用 |
|---|---|---|
| `FlyLayerView` | 全屏 | 只画，`FLAG_NOT_TOUCHABLE` |
| `FlyTouchTarget` | 只有虫子那么大 | 罩在虫子上，专门吃触摸 |

虫子多时用 **6 个替身**分摊到不同虫子上 —— 1 个替身只能让 1 只虫子可戳，
50 只时其余 49 只怎么点都没反应，手感就"死"了。

代价是这 6 块「虫子大小」的区域会拦截触摸，其余屏幕照常穿透。
**这是数量和手感之间的折中**，也是这个项目里最需要权衡的一处。

### 嗡嗡声

`BuzzPlayer` 里没有音频文件，在内存里算 PCM 推给 `AudioTrack`：
176Hz 锯齿波（蝇翅振动的主导谐波）+ 二次谐波 + 28Hz 振幅颤动。
取 0.25 秒整数个周期循环，接缝处不会有"咔哒"声。

音量不固定，由**实际位移速度**驱动 —— 乱窜时响，停落时几乎没声。

### 运动方式

四种 `Locomotion`，走不同分支：

- **FLY** 蝇类：巡航 → 窜逃 → 偶尔落停
- **CRAWL** 蟑螂/蚂蚁/瓢虫/臭虫/虱子/衣鱼：绝大多数时间在地上爬，翅膀收拢
- **HOP** 蟋蟀/跳蚤：爬行为主，`dartMul` 很高，突然弹跳式冲刺
- **HOVER** 蜻蜓/蝶/蛾/蜂：飞行为主，会在空中反复停住悬停，很少落地

---

## 开发环境的坑

### 1. 杀毒软件会拦 PowerShell 的网络

`Invoke-WebRequest` 全部秒失败，报"无法连接"。
探测发现：DNS 能解析、能 ping 通 8.8.8.8，但**连 DNS 的 53 端口都 0ms 被拒** ——
本机防火墙直接 REJECT。真凶是火绒（`HipsTray`），它只放行了浏览器。

**换 `curl.exe` 立刻 HTTP 200。** 这个区别是整条环境安装链路能否跑通的关键。

### 2. 源站限速差 30 倍

同一时刻实测：

| 源 | 速度 |
|---|---|
| Adoptium（直连） | 84 KB/s |
| Google（直连） | 300~600 KB/s |
| **华为云镜像** | **2.4 MB/s** |

所以 JDK、Gradle、Maven 依赖全走华为云/阿里云。

### 3. PowerShell 5.1 读无 BOM 的 .ps1 会把中文当 ANSI

报 `瑁呭埌鎵嬫満` 这类乱码解析错误，非常难看出是编码问题。
**所有含中文的 .ps1 必须存成 UTF-8 with BOM。** 这个坑在本项目里踩了 4 次。

### 4. 下载中断会留下"看起来完整"的残文件

Gradle 下载在 62% 被 reset，留下 80MB 的半截 zip。
原来用"文件 > 1KB 就算下好"判断，于是**把残文件当完整的跳过**，
到解压时才炸。改成用 `System.IO.Compression.ZipFile.OpenRead` 校验结构。

### 5. Java 25 太新

安卓构建目前用不了 Java 25，必须单独装 JDK 17。
脚本装到 `D:\Android\jdk17`，**不动系统已有的 Java**。

---

## 版本历史

| 版本 | 内容 |
|---|---|
| **1.1** | 16 个品种；固定体型 0.5 倍并移除滑块；安装后自动放满；照片/文献校正 |
| 1.0 | 首个可用版本：悬浮窗、触摸穿透、5 个品种、嗡嗡声 |
